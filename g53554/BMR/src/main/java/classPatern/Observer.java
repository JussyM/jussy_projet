package classPatern;

/**
 * The interface Observer.
 */
public interface Observer {
    /**
     * Update.method to update the object given as argument
     *
     * @param arg the arg that need to be update
     */
    void update(Object arg);
}
