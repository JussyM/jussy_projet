Connexion : done 
Statement :  
	Statement stmt = connexion.createStatement();
        String sql = "SELECT id, nom, mail FROM USER";
        ResultSet res = stmt.executeQuery(sql);



while(res.next()){
         //Récupérer par nom de colonne
         int id = res.getInt("id");
         String nom = res.getString("nom");
         String adresse = res.getString("mail");
         //Afficher les valeurs
         System.out.print("ID: " + id);
         System.out.print(", Nom: " + nom);
         System.out.println(", mail: " + mail);
      }  

Methode création user : 
select mail
from User

if(attributmail!=mail dans le tableau){
String sql = "INSERT INTO \"main\".\"USER\" (\"NOM\", \"MAIL\") VALUES ('test', 'test@test.com')";
}

puis on appelle la methode qui affiche les données sur la page d'acceuil genre avec un select puis je recup les données la pour les mettre dans les attributs
prepare
select id,nom,mail
from user
where mail = ?


private static void queryName( int id) {
	var connection = DriverManager.getConnection(URL,PROPERTIES);
        var prepared = connection.prepareStatement(
            "select mail frome user where id = ?");
        )
        prepared.setString(1, id);
        var result = prepared.executeQuery();
        System.out.println(result);
}



