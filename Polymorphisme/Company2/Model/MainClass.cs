using System;

namespace Model
{
    public class MainClass
    {
        static void Main(string[] args)
        {
            Program.printWithFor(Program.init());
            Program.printWithForEach(Program.init());
            
        }
    }
}